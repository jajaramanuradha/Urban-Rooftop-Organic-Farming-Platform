let crops=getCrops();const form=document.getElementById('cropForm'),list=document.getElementById('list');function safe(s){return String(s).replace(/[&<>"']/g,x=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[x]))}function render(){list.innerHTML=crops.length?crops.map((c,i)=>`<div class="crop"><div><b>🌱 ${safe(c.name)}</b><br><small>${safe(c.category)} • ${safe(c.date)} • Water: ${safe(c.water)}</small></div><button class="delete" onclick="removeCrop(${i})">Delete</button></div>`).join(''):'<p class="muted">No crops added yet.</p>'}function removeCrop(i){crops.splice(i,1);localStorage.setItem('urbanFarmCrops',JSON.stringify(crops));render();updateCount()}form.addEventListener('submit',e=>{e.preventDefault();crops.push({name:name.value.trim(),category:category.value,date:date.value,water:water.value});localStorage.setItem('urbanFarmCrops',JSON.stringify(crops));form.reset();msg.textContent='Crop added successfully!';render();updateCount()});render();